# Constellation Design Spec

> _Verified against commit d7a9715 (2026-07-02). The relationship-taxonomy tables are generated by `node scripts/gen-taxonomy-doc.js`; regenerate on taxonomy changes._

## 1. Purpose

This document defines the current product behavior, architecture, data contracts, and acceptance criteria for the `constellation` tool so another developer can reimplement it without relying on the existing source code structure. A final appendix documents the current JavaScript implementation (module map and implementation-only constants) for maintainers of this codebase; everything before the appendix is platform-neutral.

The application is a browser-only contact explorer and editor for contact data stored in Apple-style vCards, Markdown contact files, or flat TSV spreadsheets. It imports `.vcf`, `.vcard`, `.md`, `.markdown`, and `.tsv` files, builds a relationship graph from explicit and inferred data, lets the user edit contacts and relationships in-browser, and exports edited data back out (vCard, Markdown, or TSV).

## 2. Product Goals

- Run entirely in the browser with no server dependency.
- Preserve Apple Contacts compatibility as closely as possible.
- Support offline import, editing, export, and session restore.
- Treat round-trip fidelity as a core requirement.
- Make contact relationships explorable visually and editable directly.
- Support multiple graph views over the same contact data.
- Support unresolved relationship references through virtual contacts.
- Let the user define a "me" contact and derive a family network from explicit relationship connectivity.

## 3. Non-Goals

- Direct integration with the macOS Contacts database.
- Cloud sync or multi-user collaboration.
- Full vCard spec coverage for every rare property.
- Lossless preservation of every unknown Apple/private field during major structured rewrites, beyond best-effort retention.

## 4. Runtime and Delivery Model

- Delivery model: static frontend app, no build step.
- Module system: native ES modules (no bundler). The HTML shell loads a vendored D3 as a classic `<script>`, then a single module entry point; the static `import` graph resolves every other module.
- Runtime: standard desktop browser.
- Backend: none.
- Persistence: browser storage only.
- Primary local data source: imported `.vcf`, `.md`, or `.tsv` file (plus optional sibling image files for Markdown photos).
- Session restore storage: IndexedDB.
- Network dependency: none at runtime. Third-party browser libraries must be vendored locally with the app.
- D3 is vendored with the app, not loaded from a CDN, so the app runs with no internet connection.
- Because it uses ES modules, the app **must be served over `http://`** (e.g. `python3 -m http.server` from the repo root); browsers will not load ES modules from `file://`. Serving locally still requires no network access beyond localhost.

## 5. High-Level User Flows

### 5.1 Import

1. User loads a `.vcf`, `.md`, or `.tsv` file.
2. User may select or drop one file or multiple files in one operation. Image files may be included alongside a Markdown export; they are matched by filename to `photo:` references in the `.md` and re-attached as contact photos.
3. App chooses the matching format adapter for each file (by extension), parses all contacts, and builds graph data.
4. Sidebar list, filters, graph, and detail panel become interactive.
5. Imported data becomes the current working set.
6. If a Markdown contact references a photo file that was not included in the import, the app reports how many photos were unresolved rather than failing.

### 5.2 Explore

1. User searches, filters, pans/zooms graph, and selects nodes.
2. User can switch graph views without reimporting contacts.
3. Selecting a node opens the detail panel.
4. Detail panel shows contact info, relationship sections, inferred links, suggestions, or group summaries depending on the selected node.

### 5.2.2 Workspace Modes

The app must support at least these workspace modes:

- `Graph`
  - the default visual network workspace
- `Table`
  - a spreadsheet/database-like workspace for faster bulk inspection and editing
  - uses the same underlying contact dataset as Graph mode
  - shares the current search query and tag filters

### 5.2.1 Graph Views

The app must support at least these graph views:

- `Connections`
  - current explicit relationship graph
  - may optionally cluster by shared organization (an organization generates inferred colleague links when 2–100 contacts share it; larger groups are skipped)
  - may optionally overlay likely family groupings based on shared family / last name (note: surname clustering may group unrelated people who merely share a name)
  - may optionally overlay likely connections based on shared hashtags from notes
  - only creates a surname cluster when 2 or more contacts share that family name
  - only creates a hashtag cluster when 2 or more contacts share that hashtag
  - must make it visually clear these are likely / unconfirmed groupings rather than explicit relationship-field links
  - must continue to show explicit relationship edges as confirmed links
- `Geographic Relationships`
  - clusters contacts by preferred address
  - address precedence:
    - `HOME`
    - `WORK`
    - any other available address
  - hierarchy order:
    - country
    - state / province
    - city
    - street
  - street grouping strips leading house numbers so neighbors on the same road share a cluster
  - contacts without a usable address may be placed in a "No Address" group

### 5.3 Edit Contact

1. User clicks `Edit Details`.
2. Contact fields become editable.
3. Relationship items may be edited inline.
4. User saves changes.
5. In-memory contact model and backing `rawVCard` are updated.
6. Graph and list refresh immediately.

Additionally, most displayed fields on the read-mode contact card are **click-to-edit inline** without entering full edit mode (see §12.8).

### 5.4 Edit Relationships

1. User edits an existing relationship or adds a new one.
2. User may choose an existing contact or type a freeform name.
3. App updates relationship data and regenerates the corresponding Apple `itemN` vCard groups from the model.
4. Changes persist into export and session restore.

### 5.5 Export

1. User exports a single contact, the current multi-select, or the full dataset, in any of three formats (vCard, Markdown, TSV). The full dataset can additionally be exported as **one self-contained `.vcf` file per contact**.
2. vCard export is generated from current `rawVCard` blocks when available, or from standard contact fields for non-vCard imports.
3. Markdown export is generated from current contact records, standard fields, custom fields, and preserved Markdown body content; embedded photos are written as separate image files alongside the `.md`.
4. TSV export writes a flat tab-separated table; a "TSV Template" action downloads a blank, column-labeled template with a worked example row.
5. Bulk (multi-contact) export filenames carry the date.

### 5.6 Restore Last Session

1. User reloads the page or reopens the app.
2. User clicks `Restore Last`.
3. App reloads the last saved working source data plus saved UI state.

### 5.7 Switch Theme

1. User toggles the theme control in the header.
2. App switches between dark (default) and light themes and recolors the graph immediately.
3. The choice persists across reloads (see §16.3).

### 5.8 Assign Gender (wizard)

1. User opens the `Assign Gender` tool from the header.
2. A three-column board (Male | Unknown | Female) shows one draggable tile per contact, placed by its current gender.
3. Tiles can be dragged between columns, or multi-selected and reassigned via keyboard (`m` = Male, `f` = Female, `u` = Unknown/clear).
4. All changes are queued; nothing is written to contacts until `Commit Changes`. `Reset` restores every tile to its saved gender.
5. Gender values are vCard sex codes: `M` | `F` | `''` (unknown). Gender drives gendered relationship suggestions (§10.6) and gendered reciprocal types.

## 6. Core Functional Requirements

### 6.1 Import and Parse

The app must:

- Accept Apple Contacts-compatible `.vcf` files.
- Accept Markdown contact files with YAML-style frontmatter, optionally accompanied by sibling image files that resolve externalized `photo:` filename references.
- Accept flat TSV (`.tsv`) contact spreadsheets.
- Accept multiple supported files in one import operation and combine their contacts into one working set.
- Parse multiple `BEGIN:VCARD` / `END:VCARD` blocks.
- Preserve per-contact raw card text for later rewrite/export.
- Correctly unfold folded vCard lines.
- Parse Apple grouped `itemN` properties.
- Preserve or reconstruct photos.
- Support contacts that have `N:` even if `FN:` is missing.
- Assign each contact a **deterministic, stable id** derived from its `UID` (or `FN` when no UID), with an occurrence suffix to keep duplicates distinct, so ids are identical across reparses (see §10 and `ContactRecord.assignStableId`). Ids must not be random.
- Isolate per-record parse failures: a single malformed record is skipped with a console warning and the remaining records still import (see §17).

### 6.2 Contact Fields Supported

The editable contact model must support at minimum:

- Display name (`FN`)
- Structured name parts from `N`
  - given / first
  - additional / middle
  - family / last
  - prefix
  - suffix
- nickname
- maiden name
- phonetic first / last / organization name
- organization
- department (second `ORG` component)
- title
- gender (vCard sex code `M` / `F` / unset)
- birthday
- alternate/lunar birthday (display + preserve; the raw line, including `CALSCALE`, survives edits verbatim)
- anniversary
- additional custom-labeled dates (Apple date item groups beyond anniversary), each `{ label, value }`
- notes
- emails
- phones
- addresses
- websites
- instant-message handles, each `{ value, service, types, label }`
- social profiles, each `{ url, service, username, label }`
- photo
- Apple related names
- `X-ABSHOWAS:COMPANY`

### 6.3 Relationship Graph

The app must build graph nodes and edges from:

- Explicit Apple related-name relationships.
- Optional inferred org-based coworker links.
- Virtual nodes for unresolved related names.
- Mode-specific group nodes for non-default graph views when needed.

The graph must support:

- zoom and pan
- node selection
- immediate visual update after edit
- full contact names as labels
- photo rendering when available
- graph view switching
- cluster hulls for grouped graph views
- large cluster-hull labels that identify the grouping meaning
- cluster-hull labels that remain legible even when zoomed out significantly
- an on-canvas control cluster: zoom in, zoom out, fit-to-screen, reset zoom, and **re-layout** (re-runs the full force layout when the graph has drifted or spread out)

### 6.4 Filtering

The app must support:

- text search
- contact-list sort mode:
  - `First Last`
  - `Last, First`
  - rendered as an inline label/control row in the sidebar
- graph view selection
- toggle organization clustering on/off
- toggle surname clustering on/off
- toggle hashtag clustering on/off
- toggle isolated contacts on/off
- toggle virtual contacts on/off
- multi-tag filtering
- special system filters:
  - `My Family`
  - `Company`
  - `Virtual`
  - `None`
- dynamic hashtag filters derived from hashtags found in contact notes

### 6.5 Table Mode

The table workspace must support:

- switching between graph and table without reimporting contacts
- using the current search query and active tag filters
- sortable columns
- inline editing of core scalar fields
- inline structured editing of emails, phones, websites, addresses, instant messages, social profiles, and relationships
- preservation of multi-value metadata while editing in table mode:
  - built-in type labels
  - custom type labels
  - preferred status
  - hidden/default vCard types such as `INTERNET`, `VOICE`, and `PREF`
- inline editing of notes
- add-contact action
- delete-contact action
- open-contact action to jump back into graph/detail workflows

The current implementation edits these columns directly in table mode:

- name
- nickname
- organization
- department
- title
- emails
- phones
- instant messages
- social profiles
- websites
- addresses
- birthday
- anniversary
- relationships (name + type per entry; entries keep their `uid` when present)
- other dates (custom-labeled date items)
- notes

Tags are shown as a derived read-only column from note hashtags.

### 6.6 Session Persistence

The app must persist and restore:

- current edited working source data and source format
- selected “me” contact
- organization/likely-family/likely-connections/isolated/virtual toggle state
- contact-list sort mode
- graph view
- workspace mode (graph or table)
- sidebar controls-column collapse state
- enough information to reconstruct the current dataset after reload

## 7. Information Architecture

## 7.1 Main Layout

- Header bar
- Left sidebar
- Main workspace
  - Graph mode
  - Table mode
- Right detail panel

## 7.2 Header

Expected controls (consolidated into dropdown menus to keep the bar uncluttered):

- **Import ▾** menu — "Import vCard" (`.vcf`/`.vcard`), "Import Markdown" (`.md`/`.markdown` plus optional sibling image files so externalized photos import too), "Import TSV" (`.tsv`), and "Download TSV Template"
- **Export All ▾** menu (shown once contacts are loaded) — "Export All as vCard", "Export All as Individual vCards" (one `.vcf` per contact, written together via the directory picker where available, otherwise sequential downloads), "Export All as Markdown", "Export All as TSV"
- **Bulk Normalize** (rule-based multi-contact edit)
- **Assign Gender** (opens the gender wizard, §5.8)
- **Session ▾** menu — "Restore Last Session" + "Clear Saved Session"; the whole menu is disabled when no saved session exists
- light/dark theme toggle (its label announces the theme you will switch *to*)
- **Focus on Me** — centers the graph on AND selects/opens the chosen "me" contact. Disabled (grayed, unclickable) when no "me" contact is chosen or it is not in the current graph. (There is no "reset to whole graph" fallback on this button; whole-graph fit/reset lives in the on-canvas graph controls, §6.3.)
- **graph/table** view shown as a single segmented toggle (selecting one deselects the other)
- compact stats summary (contacts / visible / explicit / connections)

Dropdown menus are a shared popover component: clicking the trigger opens a list of actions; it closes on selection, outside click, or Escape.

## 7.3 Sidebar

Expected structure:

- left controls column (each titled section — "Me / Family Network", "Filter by Tag", "Display Options" — is **collapsible** by clicking its title; a chevron indicates state):
  - “me” selector / family network selector
  - category/tag filters
  - graph view selector
  - graph visibility toggles
- right contacts column:
  - search
  - contact-list header
  - contact-list sort control
  - a selection bar shown when one or more contacts are selected: **Export Selected ▾** menu (vCard / Markdown / TSV), a **Delete Selected** action, and a Clear button
  - contact list

The left controls column should be collapsible independently.

- collapsing it hides the controls column against the left window edge
- the right contacts column remains visible
- collapsing it frees more horizontal space for the graph canvas
- the collapsed/expanded state should persist with session restore

When no contacts are loaded, the graph area shows an **empty-state onboarding** panel ("Import to get started", drag-and-drop hint, and a "Choose File…" button). The graph **legend** overlay is collapsible by clicking its title.

The contact list must support two coupled display/sort modes:

- `First Last`
  - sort primarily by the standard display name
  - display names in normal display-name format
- `Last, First`
  - sort primarily by structured family name, then given name
  - display names in `Last, First` format when structured name parts are available
  - gracefully fall back to the normal display name when structured parts are incomplete

## 7.4 Detail Panel

Expected sections:

- header with photo/avatar and identity summary, plus a "center graph on this contact" button and a close button
- editable contact details — in read mode each field shows its **label above the value** (label as a small caption, then the value); most values are click-to-edit inline (§12.8)
- notes
- `Relationships` — a **collapsible** master section whose body nests, in order:
  - the contact's own relationships
  - `Referenced in Others' Cards` — a **collapsible** subsection (with a count badge)
  - `From "<org>" (inferred)` when applicable
  - `Suggested Additions` — a **collapsible** subsection (with a count badge)
- footer actions — including a single **Export Contact ▾** menu (vCard / Markdown / TSV)

The outer "Connections" or "Relationship Network" heading is not required and should be omitted.

The inferred subsection shows the first several inferred colleagues and a "+ N more" affordance that is a **clickable link** revealing the remaining entries in place.

Collapse state for the `Relationships` master section and the `Referenced in Others' Cards` and `Suggested Additions` subsections persists across contact re-selection within a session.

Footer buttons are context-sensitive: a virtual node shows "Create Contact"; edit mode shows Save/Cancel; group nodes hide the edit/delete/export actions.

## 8. Data Model

## 8.1 Parsed Contact Contract

Each parsed contact should support a model equivalent to:

```ts
type StructuredName = {
  family: string
  given: string
  additional: string
  prefix: string
  suffix: string
}

type LabeledValue = {
  value: string
  types: string[]
  isPreferred: boolean
}

type AddressValue = {
  street: string
  city: string
  state: string
  postalCode: string
  country: string
  poBox?: string
  extended?: string
  types: string[]
  isPreferred: boolean
}

type RelatedValue = {
  name: string
  type: string
  rawType?: string
  uid?: string      // rename-proof target reference when known
}

type ImValue = {
  value: string
  service?: string
  types: string[]
  label?: string
}

type SocialProfileValue = {
  url: string
  service?: string
  username?: string
  label?: string
}

type DatedValue = {
  label: string
  value: string     // YYYY-MM-DD
}

type Contact = {
  id: string
  uid?: string
  fn: string
  name: StructuredName
  nickname: string
  maidenName: string
  phoneticFirst: string
  phoneticLast: string
  phoneticOrg: string
  org: string
  department: string
  title: string
  gender: 'M' | 'F' | ''
  isCompany: boolean
  emails: LabeledValue[]
  phones: LabeledValue[]
  urls: LabeledValue[]
  addresses: AddressValue[]
  ims: ImValue[]
  socialProfiles: SocialProfileValue[]
  birthday?: string
  altBirthday?: string        // preserved verbatim through edits
  anniversary?: string
  dates: DatedValue[]         // custom Apple date items beyond anniversary
  notes: string
  related: RelatedValue[]
  photo?: string
  tags: string[]
  noteTags: string[]
  customFields: Record<string, TypedField>
  sourceDocuments: SourceDocument[]
  record: ContactRecord
  rawVCard: string
}
```

Phase 1 of multi-format support keeps this legacy contact shape as the app-facing model, but every real contact should also carry a format-neutral `ContactRecord` through `contact.record`. Existing graph, detail, table, and vCard code may continue reading the legacy fields directly while new import/export adapters target `ContactRecord`.

The standard-field list has a **single source of truth** (a shape registry with a default value per field); the parser's empty-contact initializer, the adapters, and the graph node builder all derive their field lists from it, so adding a standard field is a one-line change.

## 8.1.1 Format-Neutral ContactRecord Contract

```ts
type TypedField = {
  type: 'string' | 'number' | 'boolean' | 'date' | 'list' | 'object' | 'markdown' | 'unknown' | string
  value: unknown
  label?: string
  metadata?: Record<string, unknown>
}

type SourceDocument = {
  format: 'vcard' | 'markdown' | string
  raw: string
  index?: number | null
  dirty: boolean
}

type ContactRecord = {
  schema: 'constellation.contact'
  version: 1
  id: string
  uid?: string | null
  displayName: string
  standard: {
    // mirrors every standard field in §8.1 (fn, name, nickname, maidenName,
    // phonetic*, org, department, title, gender, isCompany, emails, phones,
    // urls, addresses, ims, socialProfiles, birthday, altBirthday,
    // anniversary, dates, related, photo, tags, noteTags, …)
    notes: string[]
  }
  fields: Record<string, TypedField>
  sourceDocuments: SourceDocument[]
}
```

`fields` is the semantic-lossless extension point for arbitrary imported Markdown fields and future non-vCard data. Unknown fields must remain attached to the contact even when the current UI does not understand or edit them.

`sourceDocuments` records the original backing representation. For vCard imports this contains the raw vCard block. For Markdown imports it contains the normalized source Markdown or the parsed frontmatter/body representation needed by the Markdown adapter.

Whenever legacy contact fields or `rawVCard` change, the attached `ContactRecord` must be refreshed so `record.standard`, `record.fields`, and `record.sourceDocuments` stay synchronized.

## 8.1.2 Contact Format Adapter Contract

File formats should enter and leave the app through small adapters rather than directly through UI/controller code.

```ts
type ContactFormatAdapter = {
  id: string
  label: string
  extensions: string[]
  mimeType: string
  canImportFile(file: File | { name: string }): boolean
  parse(text: string, options?: object): Contact[]
  serialize(contacts: Contact[], ids?: Set<string> | string[] | null): string
  exportBlob(contacts: Contact[], ids?: Set<string> | string[] | null): Blob | null
}
```

The current implementations are:

- `VCardAdapter`
  - delegates parsing to the vCard parser
  - serializes from current `rawVCard` blocks, with a full-regeneration fallback for contacts imported from a non-vCard format (see §13.1)
  - the fallback round-trips non-`company` tags via `CATEGORIES` and arbitrary custom fields via an `X-CONSTELLATION-FIELD` property
  - keeps the existing app-facing contact model stable
- `MarkdownAdapter`
  - parses YAML-style frontmatter plus Markdown body content
  - maps known fields into the legacy app-facing contact model
  - preserves unknown top-level and `fields` values as typed `customFields`
  - accepts a `photoMap` option (filename → data URL) so externalized photo references resolve on import; contacts whose referenced photo file is missing are flagged so the UI can report the count
  - serializes contacts as one Markdown document or a delimiter-separated Markdown bundle, externalizing photos as separate image files (see §13.4)
- `TsvAdapter`
  - parses a flat tab-separated table (one contact per row) into the contact model
  - serializes the selected contacts back to TSV and provides a downloadable template
  - intentionally simplified for spreadsheet editing; see §8.1.4

Future formats should be implemented as sibling adapters rather than branching format-specific behavior through the controller. The controller picks an adapter for an imported file by extension (`canImportFile`) and never branches on format itself. Files that match no adapter but are images are treated as sibling photos for Markdown import.

### 8.1.3 Markdown Contact Format

Markdown contacts use YAML-style frontmatter for structured data and the Markdown body for long-form human notes or narrative content.

```md
---
constellation: 1
uid: jane-md
fn: Jane Markdown
name:
  given: Jane
  family: Markdown
emails:
  - value: jane@example.com
    types: [HOME, INTERNET]
related:
  - uid: john-md
    name: John Markdown
    type: spouse
fields:
  favorite_color:
    type: color
    value: "#3366cc"
emergency_priority: 2
---
# Notes

Markdown body #neighbor
```

Known frontmatter keys map into standard contact fields. Unknown top-level keys and all entries under `fields` become typed custom fields. The Markdown body is preserved in `customFields.markdown_body` with `type: markdown`; when no explicit `notes` frontmatter is present, the body also populates the app-facing `notes` field so search, hashtags, and graph filters can use it.

Multiple contacts may be stored in one `.md` bundle using:

```md
<!-- CONSTELLATION:CONTACT -->
```

as the document delimiter.

### 8.1.4 TSV Contact Format

TSV is a flat, spreadsheet-friendly format for bulk editing. One contact per row; the first non-empty line is a header row that names the columns. The column order is fixed (`TsvAdapter.COLUMNS`):

```text
uid  prefix  first  middle  last  suffix  display_name  organization  title
is_company  emails  phones  street  city  state  zip  country  address_type
birthday  anniversary  urls  relationships  tags  notes
```

Encoding rules:

- Multi-valued fields (`emails`, `phones`, `urls`, `relationships`) are a `' | '`-joined list. Each item may carry a type in brackets, e.g. `[home] jane@x.com | [work] jane@y.com`; for relationships the bracket is the relationship type and the value is the target name, e.g. `[spouse] John Doe | [child] Sam`.
- A single address is spread across the `street`/`city`/`state`/`zip`/`country`/`address_type` columns. On export the preferred (home > work > first) address is written.
- `is_company` is `TRUE`/`FALSE` (truthy import accepts `true`/`yes`/`1`).
- `tags` is a `' | '`-joined list; `notes` carries hashtags, which regenerate the note-tag filters on import.
- Tabs and newlines inside any value are backslash-escaped so each contact stays on one physical row.

TSV is intentionally lossy relative to vCard/Markdown: one address per contact, a single primary type per value, and no photo. vCard and Markdown remain the lossless round-trip formats. A "TSV Template" action downloads the header row plus one worked example row to fill in.

## 8.2 Graph Node Contract

```ts
type GraphNode = {
  id: string
  label: string
  category: string
  filterTags: string[]
  isVirtual: boolean
  isGroupNode?: boolean
  groupKind?: string       // 'likely-surname' | 'likely-tag' | 'geo-country' | 'geo-state' | 'geo-city' | 'geo-street'
  groupDepth?: number      // geo hierarchy depth: 1=country … 4=street
  memberIds?: string[]
  inferredOnly?: boolean
  connectionCount?: number // incident edge count (drives node radius)
  photo?: string
  contact?: Contact
}
```

Nodes copy the standard contact fields across (deriving the copied list from the shared standard-field registry) and then add the graph-only fields, so the node shape tracks the contact shape automatically.

## 8.3 Edge Contract

```ts
type GraphEdge = {
  source: string
  target: string
  type: string
  rawType?: string
  label: string
  reverseLabel?: string    // only set when the pair passes the valid-reciprocal check (§10.5)
  inferred: boolean
  category: string         // 'family' | 'friend' | 'work' | 'neighbor' | 'other' — from the taxonomy
  org?: string
  edgeKind?: string        // 'explicit' | 'likely-surname' | 'likely-tag' | 'likely-family'
                           //   | 'geographic-hierarchy' | 'geographic-membership'
  confidence?: number      // 0.0–1.0 for unconfirmed likely edges (0.45 surname, 0.38 hashtag)
  isConfirmed?: boolean
}
```

## 8.4 Graph Hull Contract

```ts
type GraphHull = {
  id: string
  label: string
  memberIds: string[]
  kind: string
  depth?: number
  color?: string
}
```

Hull seeds are only promoted to rendered hulls when 2 or more of their members are visible.

## 9. Parsing Specification

### 9.1 Supported vCard Properties

The parser must read at least:

- `FN`
- `N`
- `UID`
- `NICKNAME`
- `ORG` (first component → organization, second component → department)
- `TITLE`
- `GENDER` (sex component `M`/`F`, anything else → unset)
- `EMAIL`
- `TEL`
- `ADR`
- `URL`
- `IMPP` (instant-message handles; an item-grouped `IMPP` may carry its service as a sibling property)
- `NOTE`
- `BDAY`
- `PHOTO`
- `X-ABSHOWAS`
- `X-ABRELATEDNAMES`
- `X-ABLabel`
- `X-ABDATE` (anniversary plus additional custom-labeled dates)
- `X-ALTBDAY` (alternate/lunar birthday; preserved verbatim including `CALSCALE`)
- `X-MAIDENNAME`
- `X-PHONETIC-FIRST-NAME` / `X-PHONETIC-LAST-NAME` / `X-PHONETIC-ORG`
- `X-SOCIALPROFILE`
- `CATEGORIES` (round-trips non-`company` tags)
- `X-CONSTELLATION-FIELD` (round-trips arbitrary typed custom fields for contacts that originated in a non-vCard format)

Parsing edge cases: `EMAIL` values that are actually stray base64 photo data (starting `/9j/`) are skipped; year-less `BDAY` values prefixed `//` are skipped.

### 9.2 Structured Names

Rules:

- Parse `N` into five structured fields.
- Split `N` on unescaped semicolons only.
- Preserve escaped semicolons inside name parts, for example `N:Doe\;Smith;Jane;;;` parses family name as `Doe;Smith`.
- Parse `FN` as display name.
- If `FN` is absent, synthesize display name from the structured name fields.
- Do not rely on token-splitting `FN` to recreate `N` except as a last-resort fallback for newly created contacts.

### 9.3 Multi-Value Entries

For emails, phones, addresses, URLs, instant messages, and social profiles:

- Parse Apple `itemN` grouping.
- Parse and normalize type labels.
- Support repeated `TYPE=` parameters, for example `TEL;TYPE=HOME;TYPE=VOICE`.
- Support comma-separated `TYPE` values, for example `TEL;TYPE=HOME,VOICE`.
- Treat bare legacy type parameters such as `;HOME` as type labels.
- Preserve custom types when present.
- Preserve preferred status.
- Split `ADR` on unescaped semicolons only so escaped semicolons inside street/address fields are preserved; preserve the PO-box and extended-address components through edits.
- For `ORG`, use the first unescaped semicolon-delimited organization component as the primary organization (second component → department) while preserving escaped semicolons inside the value.

### 9.4 Internal Type Handling

The UI must hide these internal/default types from the editable visible label field:

- Emails: `INTERNET`, `PREF`
- Phones: `VOICE`, `PREF`
- Addresses: `PREF`
- URLs: `PREF`

These hidden types must still be reintroduced during serialization where needed.

The user-facing standard type lists for emails / phones / urls / addresses come from one shared contact-method type taxonomy (ordered vCard-TYPE → display-label pairs; e.g. `CELL` displays as "Mobile", `FAX` combinations as "Home Fax"/"Work Fax"), consumed by both the field editors and the Markdown adapter.

### 9.5 Notes

- Treat notes as editable plain text.
- Preserve newlines.
- Best-effort rewrite is acceptable.

### 9.6 Photos

- Support inline vCard photos.
- Preserve or rewrite photo as a data-backed vCard field.
- Show photo immediately in detail panel and graph after edit.
- Map photo `TYPE` parameters to/from MIME types for at least PNG, GIF, WEBP, HEIC, HEIF, BMP, and TIFF; default to JPEG when the type is unknown.

### 9.7 Shared vCard Utility Semantics

Parsing and writing must use one shared set of vCard helper rules so import and export stay symmetric:

- unfold folded content lines by removing CRLF/LF followed by space or tab
- locate the property/value separator at the first colon that is not inside a quoted parameter
- split property parameters on unquoted semicolons
- split structured values such as `N` and `ADR` on unescaped semicolons
- decode escaped value characters:
  - `\n` and `\N` become a newline
  - `\,` becomes comma
  - `\;` becomes semicolon
  - `\\` becomes backslash
- encode exported values by escaping backslash, semicolon, comma, and newlines
- fold generated content lines to vCard-safe continuation lines before storing them in `rawVCard`
  - folding must respect the vCard 75-octet limit, not just JavaScript character count
  - folding must not split multibyte UTF-8 characters
- use CRLF line endings for generated vCard output
- provide a stable **contact-method key** for a parsed multi-value instance (property + normalized value), used to match model instances back to their original raw lines during rewrite (§13.1)

## 10. Relationship Resolution Rules

### 10.1 Explicit Relationships

Relationships are based on Apple related-name fields.

Each outgoing related-name entry belongs to the contact whose card contains it.

A relationship target is resolved **UID-first**:

1. If the relationship entry carries a `uid` and a contact with that UID exists, resolve to it. This is exact and rename-proof; Markdown and TSV relationships can carry a `uid`, and the add-relationship flow stores the target's UID when the target is an existing contact.
2. Otherwise fall back to name matching, subject to the duplicate-name rule below.
3. If neither resolves, the target becomes a virtual node.

### 10.2 Duplicate Names

Name collisions must never silently resolve to a single contact.

Required behavior:

- maintain a name index that can return multiple matches; index each contact under at least four normalized keys:
  1. exact display name (lowercased)
  2. display name with quoted nickname tokens removed
  3. `Last, First` form
  4. `First Last` only (dropping middle names for 3+ word names)
- only auto-resolve when exactly one real contact matches
- otherwise treat the relationship target as ambiguous/unresolved (log a warning when an index key maps to 2+ contacts)
- prefer creating or keeping a virtual node over linking to the wrong real person

### 10.3 Virtual Contacts

Virtual contacts represent unresolved names referenced by explicit relationships.

They must:

- appear in graph and details
- be distinguishable from real contacts
- support conversion into a new real contact

### 10.4 Family Filter

`My Family` is not defined as “has a family-type relationship.”

It is defined as:

- choose one real contact as “me”
- construct an undirected graph of explicit relationship edges only
- compute the connected component containing “me”
- every node in that component receives the `family` filter tag

Implications:

- inferred org edges do not affect family membership
- a person may have family-type labels and still not be in the `My Family` filter
- any explicit relationship type can contribute to connectedness

### 10.5 Relationship Taxonomy (generated)

The full relationship-type taxonomy below is the single source of truth for relationship semantics: canonical types, display labels, Apple `X-ABLabel` values, edge categories, reciprocals, generic parents (used by the reciprocal-downgrade guard and the indented picker), import aliases, gender groups, and valid reciprocal pairs. It is **generated by `node scripts/gen-taxonomy-doc.js`** from the taxonomy module and must be regenerated whenever the taxonomy changes.

#### Relationship types

| Type (canonical key) | Display label | X-ABLabel | Category | Reciprocal | Generic parent |
| --- | --- | --- | --- | --- | --- |
| `spouse` | Spouse | `_$!<Spouse>!$_` | family | `spouse` | — |
| `husband` | Husband | `_$!<Husband>!$_` | family | `wife` | `spouse` |
| `wife` | Wife | `_$!<Wife>!$_` | family | `husband` | `spouse` |
| `partner` | Partner | `_$!<Partner>!$_` | family | `partner` | — |
| `mother` | Mother | `_$!<Mother>!$_` | family | `child` | `parent` |
| `father` | Father | `_$!<Father>!$_` | family | `child` | `parent` |
| `parent` | Parent | `_$!<Parent>!$_` | family | `child` | — |
| `stepmother` | Stepmother | `_$!<Stepmother>!$_` | family | `stepchild` | `stepparent` |
| `stepfather` | Stepfather | `_$!<Stepfather>!$_` | family | `stepchild` | `stepparent` |
| `stepparent` | Stepparent | `_$!<Stepparent>!$_` | family | `stepchild` | — |
| `son` | Son | `_$!<Son>!$_` | family | `parent` | `child` |
| `daughter` | Daughter | `_$!<Daughter>!$_` | family | `parent` | `child` |
| `child` | Child | `_$!<Child>!$_` | family | `parent` | — |
| `stepson` | Stepson | `_$!<Stepson>!$_` | family | `stepparent` | `stepchild` |
| `stepdaughter` | Stepdaughter | `_$!<Stepdaughter>!$_` | family | `stepparent` | `stepchild` |
| `stepchild` | Stepchild | `_$!<Stepchild>!$_` | family | `stepparent` | — |
| `brother` | Brother | `_$!<Brother>!$_` | family | `sibling` | `sibling` |
| `sister` | Sister | `_$!<Sister>!$_` | family | `sibling` | `sibling` |
| `sibling` | Sibling | `_$!<Sibling>!$_` | family | `sibling` | — |
| `grandmother` | Grandmother | `_$!<Grandmother>!$_` | family | `grandchild` | `grandparent` |
| `grandfather` | Grandfather | `_$!<Grandfather>!$_` | family | `grandchild` | `grandparent` |
| `grandparent` | Grandparent | `_$!<Grandparent>!$_` | family | `grandchild` | — |
| `grandson` | Grandson | `_$!<Grandson>!$_` | family | `grandparent` | `grandchild` |
| `granddaughter` | Granddaughter | `_$!<Granddaughter>!$_` | family | `grandparent` | `grandchild` |
| `grandchild` | Grandchild | `_$!<Grandchild>!$_` | family | `grandparent` | — |
| `uncle` | Uncle | `_$!<Uncle>!$_` | family | `nephew` | — |
| `aunt` | Aunt | `_$!<Aunt>!$_` | family | `niece` | — |
| `nephew` | Nephew | `_$!<Nephew>!$_` | family | `uncle` | — |
| `niece` | Niece | `_$!<Niece>!$_` | family | `aunt` | — |
| `cousin` | Cousin | `_$!<Cousin>!$_` | family | `cousin` | — |
| `friend` | Friend | `_$!<Friend>!$_` | friend | `friend` | — |
| `neighbor` | Neighbor | `_$!<Neighbor>!$_` | neighbor | `neighbor` | — |
| `colleague` | Colleague | `_$!<Colleague>!$_` | work | `colleague` | — |
| `manager` | Manager | `_$!<Manager>!$_` | work | `assistant` | — |
| `assistant` | Assistant | `_$!<Assistant>!$_` | work | `manager` | — |
| `step-parent` | Stepparent | `_$!<Stepparent>!$_` | family | `stepchild` | — |
| `step-child` | Stepchild | `_$!<Stepchild>!$_` | family | `stepparent` | — |
| `uncle/aunt` | Uncle/Aunt | `_$!<Uncle>!$_` | family | `nephew/niece` | — |
| `nephew/niece` | Nephew/Niece | `_$!<Nephew>!$_` | family | `uncle/aunt` | — |

#### Aliases (normalized on import)

| Alias | Canonical type |
| --- | --- |
| `domestic partner` | `partner` |
| `step mother` | `stepmother` |
| `step father` | `stepfather` |
| `step parent` | `stepparent` |
| `step-parent` | `stepparent` |
| `step son` | `stepson` |
| `step daughter` | `stepdaughter` |
| `step child` | `stepchild` |
| `step-child` | `stepchild` |
| `grand mother` | `grandmother` |
| `grand father` | `grandfather` |
| `grand parent` | `grandparent` |
| `grand son` | `grandson` |
| `grand daughter` | `granddaughter` |
| `grand child` | `grandchild` |
| `best friend` | `friend` |
| `coworker` | `colleague` |
| `co-worker` | `colleague` |
| `boss` | `manager` |

#### Gender groups (concept → gendered variants)

| Concept | Male | Female | Neutral |
| --- | --- | --- | --- |
| `spouse` | `husband` | `wife` | `spouse` |
| `parent` | `father` | `mother` | `parent` |
| `stepparent` | `stepfather` | `stepmother` | `stepparent` |
| `child` | `son` | `daughter` | `child` |
| `stepchild` | `stepson` | `stepdaughter` | `stepchild` |
| `sibling` | `brother` | `sister` | `sibling` |
| `grandparent` | `grandfather` | `grandmother` | `grandparent` |
| `grandchild` | `grandson` | `granddaughter` | `grandchild` |
| `pibling` | `uncle` | `aunt` | — |
| `nibling` | `nephew` | `niece` | — |

#### Valid reciprocal pairs

An edge is labeled with both endpoint roles only when the pair appears here (guards against data-entry errors):

- `spouse` ↔ `spouse`
- `husband` ↔ `wife`
- `husband` ↔ `spouse`
- `wife` ↔ `spouse`
- `partner` ↔ `partner`
- `friend` ↔ `friend`
- `colleague` ↔ `colleague`
- `neighbor` ↔ `neighbor`
- `cousin` ↔ `cousin`
- `manager` ↔ `assistant`
- `mother` ↔ `son`
- `mother` ↔ `daughter`
- `mother` ↔ `child`
- `father` ↔ `son`
- `father` ↔ `daughter`
- `father` ↔ `child`
- `parent` ↔ `son`
- `parent` ↔ `daughter`
- `parent` ↔ `child`
- `stepmother` ↔ `stepson`
- `stepmother` ↔ `stepdaughter`
- `stepmother` ↔ `stepchild`
- `stepfather` ↔ `stepson`
- `stepfather` ↔ `stepdaughter`
- `stepfather` ↔ `stepchild`
- `stepparent` ↔ `stepson`
- `stepparent` ↔ `stepdaughter`
- `stepparent` ↔ `stepchild`
- `step-parent` ↔ `step-child`
- `step-parent` ↔ `stepson`
- `step-parent` ↔ `stepdaughter`
- `step-parent` ↔ `stepchild`
- `stepparent` ↔ `step-child`
- `brother` ↔ `brother`
- `brother` ↔ `sister`
- `brother` ↔ `sibling`
- `sister` ↔ `sister`
- `sister` ↔ `sibling`
- `sibling` ↔ `sibling`
- `grandmother` ↔ `grandson`
- `grandmother` ↔ `granddaughter`
- `grandmother` ↔ `grandchild`
- `grandfather` ↔ `grandson`
- `grandfather` ↔ `granddaughter`
- `grandfather` ↔ `grandchild`
- `grandparent` ↔ `grandson`
- `grandparent` ↔ `granddaughter`
- `grandparent` ↔ `grandchild`
- `uncle` ↔ `nephew`
- `uncle` ↔ `niece`
- `aunt` ↔ `nephew`
- `aunt` ↔ `niece`
- `uncle/aunt` ↔ `nephew/niece`
- `uncle/aunt` ↔ `nephew`
- `uncle/aunt` ↔ `niece`

Normalization strips Apple's `_$!<Label>!$_` wrapper, lowercases, and maps aliases to canonical hyphen-free keys (e.g. `stepson`, not `step-son`). Unknown labels are kept as custom types.

**Reciprocal gendering.** The reciprocal of a type is looked up from the table above, then — when the gender of the person who will *hold* the reciprocal role is known — mapped through the gender groups to the gendered variant (e.g. reciprocal of `mother` is `child`; if the target contact's gender is `M`, the suggested type becomes `son`).

**Reciprocal-downgrade guard.** When an edit auto-updates the other person's reciprocal entry, a computed reciprocal that is the *generic parent* of the existing type (per the "Generic parent" column) must not overwrite it — e.g. never replace an existing `son` with `child`.

**Picker structure.** The relationship-type picker is a single flat list (no group headers): each generic parent type (Spouse, Parent, Stepparent, Child, Stepchild, Sibling, Grandparent, Grandchild) is selectable, with its more-specific subtypes indented beneath it; standalone types (uncle, aunt, nephew, niece, cousin, friend, neighbor, colleague, manager, assistant) are flat. There is no "(generic)" label. A "Custom…" escape hatch reveals a freeform input; pickers that must yield a known type (bulk retype) omit it.

### 10.6 Relationship Suggestion Engine

For the selected contact node, the app computes relationship suggestions of six kinds. Every suggestion carries: a deduplication `key` (also used for dismissal), a `kind`, the `targetId`/`targetName` (whose card gains the relationship), the proposed `relName`/`relType`, and a human-readable `reason`. Dismissed keys are held in memory only (cleared on reload). Each rendered suggestion offers a type picker (searchable, with a Custom escape hatch) so the user can adjust the proposed type before applying. Applying a suggestion appends to the target contact's `related` list, regenerates that contact's card from the model, rebuilds the graph, and persists the session.

**Granularity.** By default only suggestions proposing *immediate-family* types are shown — spouses/partners, parents, children, and siblings (including step-variants). Inferred suggestions proposing any other type (aunts/uncles, cousins, nephews/nieces, grandparents, the generic `relative`, …) are hidden behind an "Include extended family" checkbox rendered at the top of the Suggested Additions section, which also reports how many suggestions are hidden. Reciprocal *mirror* suggestions (kind `mutual`, kinds 1 and 5 below) are exempt from the filter regardless of type — they complete relationships the user explicitly entered rather than guess. The preference (`suggestExtendedFamily`, default off) persists with the session.

| # | Kind | Trigger condition | Proposed relationship (and target card) |
|---|---|---|---|
| 1 | Mutual (outbound) | Selected contact A lists B (resolving to a real contact), but B's card does not list A back (by id or exact name). | Add A to **B's card** with `reciprocal(rel.type)`, gendered by **A's** gender. |
| 2 | Shared child | A has a `spouse`/`husband`/`wife`/`partner` relationship to real contact B; A lists child C (`son`/`daughter`/`child`/step-variants) and B does not list C in any role. | Add C to **B's card** with the **same** child type. |
| 3 | Transitive (two-hop) | A has a *pivot* relationship to P; P lists a *bridge* relationship to third party T (T ≠ A; T may be real or virtual); A does not already list T. Rules in the transition table below. | Add T to **A's card** with the rule's inferred type. |
| 4 | Reverse-parent siblings | Any contact P lists A as a child type (even if A does not list P back); P's other child-type entries name T ≠ A. | Add T to **A's card** as `brother` (bridge `son`), `sister` (bridge `daughter`), else `sibling`. |
| 5 | Inbound reciprocal | Any contact O lists A in any role, and A does not list O back. | Add O to **A's card** with `reciprocal(rel.type)`, gendered by **O's** gender. |
| 6 | Likely-cluster peer | In the Connections view with likely clusters visible: another member of a surname or hashtag group node that A belongs to, not already related to A. | Add the peer to **A's card** as type `relative`, with a reason naming the shared surname/hashtag(s). |

Gendering: reciprocal types (kinds 1 and 5) are computed via the taxonomy's gendered-reciprocal lookup — the reciprocal concept mapped through the gender groups (§10.5) to the `M`/`F` variant of whoever will hold the role, falling back to the neutral type when gender is unset. Suggestion reason text uses gendered possessive pronouns (his/her/their). Kind-3 and kind-4 types are gendered by the *bridge relationship's* gendered type (see `typeMapper` mappings below), not by contact gender.

Transitive (kind 3) rule table — "if A has ⟨pivot⟩ to P, and P has ⟨bridge⟩ to T, suggest T as ⟨suggested⟩ on A's card":

| Pivot (A→P) | Bridge (P→T) | Suggested type (A→T) |
|---|---|---|
| spouse, husband, wife, partner | son, daughter, child, stepson, stepdaughter, stepchild, step-child | same as bridge type |
| parent, mother, father, stepmother, stepfather, stepparent, step-parent | son, daughter, child, stepson, stepdaughter, stepchild, step-child | son/stepson → `brother`; daughter/stepdaughter → `sister`; else `sibling` |
| parent, mother, father | parent, mother, father, grandmother, grandfather, grandparent | mother/grandmother → `grandmother`; father/grandfather → `grandfather`; else `grandparent` |
| parent, mother, father | sibling, brother, sister | brother → `uncle`; sister → `aunt`; else `uncle` |
| son, daughter, child | son, daughter, child | son → `grandson`; daughter → `granddaughter`; else `grandchild` |
| sibling, brother, sister | son, daughter, child | son → `nephew`; daughter → `niece`; else `nephew` |
| sibling, brother, sister | parent, mother, father, grandmother, grandfather | mother/grandmother → `mother`; father/grandfather → `father`; else `parent` |
| uncle, aunt, uncle/aunt | son, daughter, child | `cousin` |
| grandmother, grandfather, grandparent | son, daughter, child | son → `uncle`; daughter → `aunt`; else `uncle` |
| nephew, niece, nephew/niece | parent, mother, father | mother → `sister`; father → `brother`; else `sibling` |
| cousin | parent, mother, father | mother → `aunt`; father → `uncle`; else `uncle` |
| grandson, granddaughter, grandchild | parent, mother, father | mother → `daughter`; father → `son`; else `child` |
| nephew, niece, nephew/niece | sibling, brother, sister | brother → `nephew`; sister → `niece`; else `nephew` |
| grandson, granddaughter, grandchild | sibling, brother, sister | brother → `grandson`; sister → `granddaughter`; else `grandchild` |
| cousin | sibling, brother, sister | `cousin` |

Implementation constraints:

- Skip suggestions whose target already lists the person in *any* role (matched by resolved id or case-insensitive name).
- Kinds 4 and 5 share one pre-built inbound-reference index (every `{contact, rel}` pointing at the selected node) so the contact list is scanned once, not once per kind.
- Third parties in kinds 3 and 4 may resolve to **virtual** contacts (a suggestion may propose linking to a not-yet-imported person); suggestions can only be *applied* to targets with editable card data.

## 11. Category and Filter Model

There are two different concepts:

- primary category for styling
- filter tags for filtering

### 11.1 Primary Category

Only one primary category is chosen for display styling.

Current implementation uses a simplified styling model:

1. virtual
2. company
3. other

### 11.2 Filter Tags

A node may have multiple filter tags simultaneously.

Both the graph and sidebar contact list must use the same filter-tag semantics.

The sidebar contact list should visually reflect a contact's active filter tags:

- system tags use fixed app colors
- note hashtags use stable deterministic colors
- multi-tag contacts may display a blended or multi-color treatment

Category and tag colors have a **single source of truth**: named color tokens defined by the theme stylesheet. A palette module reads them once (cached) and both the graph renderer and the sidebar consume the palette rather than hardcoding colors. Because the palette is stylesheet-driven, switching theme (§16.3) recolors everything by refreshing the palette cache and re-reading the tokens.

Filter tags come from two sources:

- system tags:
  - `family` (`My Family`)
  - `company`
  - `virtual`
  - `other` (`None`)
- note hashtags parsed from the contact's notes

The filter bar lists tags in a fixed order — `family`, `company`, `virtual`, `other` first, then alphabetically sorted hashtags. The `family` filter is disabled until a "me" contact is chosen.

Hashtag normalization rules:

- case-insensitive
- stored in lowercase
- leading `#` removed internally
- deduplicated per contact
- recognized by scanning note text for `#tag` tokens at a word boundary: a tag starts with an alphanumeric and may continue with alphanumerics, `_`, or `-` (pattern equivalent to `(^|[\s([{,;])#([A-Za-z0-9][A-Za-z0-9_-]*)`)

## 12. Editing Specification

## 12.1 Contact Edit Mode

When `Edit Details` is activated:

- contact scalar fields become editable
- photo controls become editable
- multi-value sections become editable
- relationship rows can enter inline edit mode
- custom fields: scalar and list custom fields are editable; nested object custom fields render read-only and remain preserved

## 12.2 Multi-Value Type Editor

For emails, phones, addresses, and websites:

- use a dropdown for known built-in types
- include `Custom` as a dropdown option
- when type is built-in, hide the custom type input
- when type is custom, show the custom type input
- dropdown should default to the current vCard type if recognized
- otherwise default to `Custom` and prefill the custom input

## 12.3 Preferred Item Behavior

For each of these groups independently:

- emails
- phones
- addresses
- websites

The UI must allow exactly one preferred item via radio selection, or zero if none is selected.

Setting one item preferred in a group must clear preferred status on the others in that same group.

### 12.4 Company Flag

Provide a checkbox to control whether the contact is serialized with:

```text
X-ABSHOWAS:COMPANY
```

### 12.5 Relationship Editing

Each explicit relationship row must support:

- editing target name
- editing relationship type
- deleting the relationship
- inline save/cancel controls

The target name control must:

- allow picking an existing contact
- allow typing a brand-new freeform name
- provide autocomplete suggestions from existing contacts

The relationship-type control presents a single flat list (no group headers): each generic parent type (Spouse, Parent, Stepparent, Child, Stepchild, Sibling, Grandparent, Grandchild) is selectable, with its more-specific subtypes (e.g. Husband / Wife / Partner under Spouse) shown **indented** beneath it. There is no "(generic)" label — selecting the parent stores the generic type, selecting a child stores that specific type. A "Custom…" escape hatch reveals a freeform input.

When relationship inline edit mode is active:

- clicking the row must not navigate to the target contact

Saving a relationship type edit also updates the reciprocal on the other person's card: resolve the target, compute the gendered reciprocal (§10.5), skip the write when it would be a reciprocal downgrade, otherwise update the target's entry and regenerate that card too. Surface the outcome ("Updated to X" / "Updated to X — also set Y's side to Z").

### 12.6 Relationship Add Flow

The `Add Relationship` action must appear at the bottom of the explicit `Relationships` section.

It must allow:

- selecting an existing contact or entering a new name
- selecting a relationship type
- persisting both model data and raw vCard changes
- when the target is an existing contact, recording that contact's `uid` on the relationship entry so future resolution is rename-proof (§10.1)

Large pickers — the existing-contact picker and the relationship-type picker — must support **type-to-filter** (start typing to narrow the list) rather than requiring the user to scroll the full list.

The `Add Relationship` action must remain available while a contact is being **edited**. Opening it mid-edit must first commit the in-progress edit-form state to the model so unsaved field changes are not lost; edit mode continues after the relationship is added.

### 12.7 Bulk Relationship Editing

The bulk-normalize tool must support relationships as a first-class field:

- a **search condition** matching contacts that have (or do not have) a relationship of a chosen type
- a **retype action** that changes relationships of one type to another across all matched contacts, **updating the reciprocal relationship on the other person's card** using the same reciprocal rules as single-contact editing (subject to the reciprocal-downgrade guard, §10.5)

Both the field/type pickers in the bulk tool support type-to-filter.

The bulk tool as a whole supports multi-condition IF rules with AND/OR logic; condition operators include contains / equals / starts-with / ends-with / is-empty / is-not-empty over contact, email, and address scopes. Actions are set / append / clear plus the relationship retype above; when the action is Clear the value input is hidden. It shows a live preview of affected contacts and a sample list, requires a confirmation checkbox, and labels the apply button "Apply Rule to N Contacts" with the live count (disabled at 0). After apply, the graph is rebuilt and the session persisted.

### 12.8 Inline Read-Mode Field Editing

Displayed values on the read-mode contact card are click-to-edit without entering full edit mode (real contacts only; virtual placeholders are not editable this way):

- Clicking an editable value swaps it for an inline editor (a text/date/select control, or a **composite stacked editor** for multi-field values such as postal addresses: Street / City / State / ZIP / Country, pre-filled) with ✓ commit / ✕ cancel controls; Enter commits, Escape cancels.
- Committing updates the contact model, regenerates the backing card, refreshes the panel, rebuilds the graph when the change can affect it (e.g. name/org), and persists the session.
- Committing an address edit preserves the untouched address metadata (label/types/preferred, PO box, extended address).
- Inline editing covers at least: full name, nickname, organization, title, gender, dates, individual email/phone/website values, and addresses.
- Notes have their own inline editor with autosave: input is debounced (~2 seconds) and flushed immediately on blur; saving updates notes, note hashtags, and tags, regenerates the card, and skips the graph rebuild entirely when no `#hashtag` changed.
- Notes fields (detail panel and table) provide hashtag autocomplete: typing `#` opens a floating popup of matching existing tags at the caret; Arrow keys navigate, Enter/Tab applies, Escape closes.

## 13. Serialization and Export

## 13.1 General Rules

All vCard writing flows through **one serializer** with two entry points sharing a single model-driven body generator:

- **Rewrite** (vCard-origin contacts, i.e. `rawVCard` present) — hybrid raw preservation. Properties the model does not cover (`PRODID`, `REV`, obscure Apple properties and item groups, …) are kept **verbatim** from the original card; modeled properties are regenerated from the contact model. Within the modeled multi-value contact methods, an *untouched* instance — whose contact-method key (§9.7) still maps to its original raw line(s) captured at parse time — is re-emitted **byte-for-byte**, preserving Apple's exact `TYPE` casing and parameter order; only edited instances are regenerated. `GENDER` and `X-CONSTELLATION-FIELD` are always dropped from the raw card and regenerated from the model (this prevents duplicate `GENDER` lines and stale custom-field payloads). The original `CATEGORIES` line is kept verbatim on rewrite so inferred tags are never written into an Apple-origin card.
- **Generate** (Markdown/TSV-origin contacts and in-app creations, no raw card) — full regeneration from the model. Additionally emits `UID`, `CATEGORIES` from non-system tags, `PHOTO`, and `X-CONSTELLATION-FIELD` custom-field payloads.

Every write path — detail/table edits, inline edits, relationship add/edit/delete, suggestions, bulk normalize, and the export fallback — must go through this serializer so formats cannot drift apart. There must be no secondary/duplicated serializer and no raw-string splicing of relationship lines.

Additional rules:

- Export from current in-memory `rawVCard` blocks when present; otherwise generate.
- Escape commas, semicolons, backslashes, and newlines correctly.
- Fold generated vCard lines before export using continuation lines that begin with a single space, respecting UTF-8 byte limits.
- Normalize rewritten cards to CRLF line endings.
- Preserve valid Apple `itemN` patterns.
- Support rewriting contact cards after edits without requiring a full parser round-trip.
- Preserve `ADR` PO-box and extended-address components through edits.

## 13.2 Structured Name Serialization

When saving a contact:

- serialize `FN` from display name
- serialize `N` from structured name fields
- preserve empty structured slots where needed

Example:

```text
N:Last;First;Middle;Prefix;Suffix
```

## 13.3 Relationship Serialization Semantics

Relationships are **model-driven**: `contact.related` is the single source of truth, and the `itemN.X-ABRELATEDNAMES` / `itemN.X-ABLabel` groups are regenerated from it by the same serializer that regenerates emails/phones/addresses, not patched in place. Add / edit / delete relationship actions mutate `contact.related` and re-run that regeneration. (Earlier versions hand-patched individual item groups with regex surgery; that dual-write is gone.)

The regeneration must:

- emit one `itemN` group per related entry, escaping the name and using the entry's raw/derived Apple label
- preserve non-relationship item groups and unknown Apple properties verbatim (see §13.5)
- tolerate folded and escaped input when reading the original card

When the user edits a relationship to a target that is an existing contact, the relationship records that contact's `uid` so future resolution is rename-proof (§10.1). Reciprocal updates on the other contact regenerate that contact's card the same way.

Saving relationship edits from the main contact `Save` action must commit any still-open inline relationship editors before serializing.

### 13.5 Rewrite Preservation Rules

When rewriting editable contact fields:

- keep unknown non-editable simple properties whenever practical
- keep non-editable `itemN` groups whenever practical
- keep the alternate-birthday line (including `CALSCALE`) verbatim
- regenerate anniversary and the model's custom date items from the contact model while preserving any remaining unmodeled Apple date item groups
- regenerate editable scalar and collection fields from the current contact model — including emails, phones, addresses, URLs, instant messages, social profiles, the anniversary item group, and the explicit relationship `itemN` groups (which are derived from `contact.related`, §13.3)
- drop and regenerate `GENDER` and `X-CONSTELLATION-FIELD` (never keep the raw lines; §13.1)
- re-emit untouched multi-value instances byte-for-byte from their captured raw lines (§13.1)
- use shared vCard encoding/folding helpers for all generated lines

## 13.6 HTML and Link Safety

Imported contact data and user-entered freeform fields must be treated as untrusted before inserting into HTML.

- Render plain contact values with `textContent` or an equivalent HTML-escaping helper.
- Use **one shared escaping helper** everywhere a value is interpolated into markup; it must escape `&`, `<`, `>`, `"`, and `'` so it is safe in both text and quoted-attribute positions.
- Escape custom relationship type display labels before rendering relationship rows.
- Do not render unsafe URL protocols, such as `javascript:`, as clickable links; only safe external protocols such as `http:` and `https:` become links, everything else renders as text.

## 13.4 Export Variants

Export is **multi-scope × multi-format**. Each scope can be exported in each format:

- scopes: single contact (detail panel), current multi-selection (export bar), all contacts (header)
- formats: vCard (`.vcf`), Markdown (`.md`), TSV (`.tsv`)

Plus:

- **Export All as Individual vCards** — one self-contained `.vcf` file per contact, written together into a user-chosen folder via the directory picker where available, otherwise as sequential downloads.
- a standalone **TSV Template** download (header row + one worked example row).

Bulk (multi-contact) export filenames carry the date, e.g. `contacts 2026-06-26.tsv`.

Virtual-only contacts cannot be exported until converted into real contacts.

Markdown export must include standard fields, typed custom fields, and preserved Markdown body content. Multi-contact Markdown exports use the `<!-- CONSTELLATION:CONTACT -->` bundle delimiter. Unknown custom field payloads must keep semantic values such as nested objects, lists, booleans, nulls, empty strings, and numeric-looking strings.

Markdown export **externalizes photos**: instead of base64-embedding a photo, the `.md` references a separate, human-readably-named image file, and the image files are written alongside the `.md`. The app writes them together using the File System Access directory picker (`showDirectoryPicker`) where available, falling back to individual downloads. A photo-free export is a single `.md`. Re-importing the `.md` together with its image files re-attaches the photos (§5.1), completing the lossless Markdown round-trip.

TSV export writes the flat table described in §8.1.4 for the chosen scope.

## 14. Browser Persistence

Session restore must use IndexedDB rather than `localStorage` because edited source payloads may exceed `localStorage` quotas.

Persisted session data must include:

- active format id
- serialized working source data
- imported file name or label
- timestamp (saved-at)
- selected “me” contact reference
- inferred (org) visibility toggle
- likely-family and likely-connections visibility toggles
- isolated visibility toggle
- virtual visibility toggle
- sidebar controls-column collapse state
- contact-list sort mode
- graph view mode
- workspace mode (graph or table)

The selected “me” reference must be stable across reparses:

- prefer `UID`
- fallback to `FN`

## 15. Graph Rendering Requirements

The graph renderer must:

- display full names for all contacts, including companies and doctors
- render person and group nodes as circles; render **company nodes as rounded squares** (corner radius ≈ 0.32 × the node's half-size) in the dedicated company category color, so companies are identifiable by silhouette or by color alone. The shape swap must apply live when a contact's company flag is toggled, and the legend's Company swatch mirrors the square shape.
- render photo when present, cropped to the node's shape (circular for people, rounded-square for companies)
- render fallback initials or icon when photo is missing (two-character initials for people; a building glyph for companies; group-kind glyphs for group nodes — `≈` surname, `#` hashtag, `◎` geographic, `◌` other)
- update node content immediately after photo edits
- distinguish inferred edges from explicit edges visually (dashed strokes for inferred/likely edges; per-category edge colors and arrowheads)
- render both edge-endpoint role labels when a valid reciprocal pair exists (§10.5), positioned ~32% along the edge for the source label and ~68% for the target label, or one centered label otherwise
- support node selection without stale detail-body rendering
- on selection, dim non-connected nodes and non-incident edges and show a selection halo on the selected node
- preserve node positions across rebuilds: a position cache seeds each node's `x`/`y` from its previous layout so an edit does not re-scatter the graph. When most nodes are already placed the simulation resettles gently with a low alpha; a genuinely new node set lays out fresh.
- provide a **re-layout** action that clears the position cache and any pinned positions, re-scatters nodes near the center, re-runs a full-energy layout, and auto-fits the view once it settles

Selecting a different node while the detail panel is open must fully rerender both header and body for the new node.

To keep editing cheap, edits that cannot change the graph topology should avoid a full rebuild. In particular, saving a note that changes no `#hashtag` updates the contact record without rebuilding the builder/graph.

### 15.1 Force-Layout and Rendering Constants

The following constants define the current look and feel and must be matched for a visually equivalent reimplementation (d3-force semantics):

| Parameter | Value |
|---|---|
| Link distance — `geographic-hierarchy` edges | 58 |
| Link distance — `geographic-membership` edges | 70 |
| Link distance — likely edges (`likely-surname` / `likely-tag` / `likely-family`) | 65 |
| Link distance — explicit `family`-category edges | 80 |
| Link distance — explicit `work`-category edges | 100 |
| Link distance — all other edges | 120 |
| Link strength — `geographic-hierarchy` | 0.9 |
| Link strength — `geographic-membership` | 0.82 |
| Link strength — likely edges | 0.76 |
| Link strength — default | 0.4 |
| Charge (many-body) — group nodes | −520 |
| Charge — company nodes | −400 |
| Charge — regular nodes | −150 |
| Charge `distanceMax` | 400 |
| Centering force | container center |
| Collision radius | node radius + 8 |
| Node radius — group nodes | `max(12, 18 − groupDepth × 1.5)` |
| Node radius — contact nodes | base (company 12, virtual 6, person 10) + `min(connectionCount × 1.5, 10)` |
| Restart alpha — incremental rebuild | 0.3 |
| Restart alpha — full layout | 1.0 |
| Incremental-mode heuristic | ≥ 50% of incoming nodes have cached positions (new nodes seed near center ± 40px) |
| Zoom scale extent | 0.05 – 4 |
| Node-label visibility threshold | labels visible only when zoom `k > 0.6` |
| Edge-label positions | 32% (source) / 68% (target) along the edge when dual-labeled; 50% when single |
| Selection dimming | non-connected nodes → opacity 0.15; non-incident edges → opacity 0.05 |
| Hull padding | convex hull of member-node corner points expanded by node radius + 12 |

Cluster hulls are convex-hull paths around member nodes; hull labels sit at the top-center of the hull bounding box and scale inversely with zoom so they remain a constant visual size (and stay legible when zoomed far out). The simulation object and its forces are created once and reused across renders (only nodes/links/center are repointed) so velocity continuity is preserved; the tick handler is re-bound per render.

Visual layering (bottom to top): hulls → hull labels → edges → nodes → name labels.

## 16. Derived / Computed UI Behavior

### 16.1 Age Display

Next to birthday and anniversary, show completed years rounded down.

Examples:

- birthday: `Jan 5, 1988 (38)`
- anniversary: `Jun 12, 2010 (15)`

### 16.2 Detail Panel Relationship Sections

`Relationships` is a collapsible master section; the following are nested subsections within its body and appear only when relevant:

- own relationships (directly under the `Relationships` header) — each row reads `[Type] is [Name]` (e.g. "Father is Chris Parker")
- `Referenced in Others' Cards` (a collapsible subsection with a count badge) — each row reads `[Type] of [Name]` (e.g. "Son of Chris Parker"); the "listed in <name>'s card" note is a hover tooltip, not a visible field
- `From "<org>" (inferred)` (with a clickable "+ N more" reveal link when truncated; shows up to 8 rows before truncating)
- `Suggested Additions` (a collapsible subsection with a count badge; see §10.6)

The connector word ("is" / "of") is static, not editable. Collapse state for the master section and the `Referenced in Others' Cards` and `Suggested Additions` subsections persists across re-selection within a session.

When the selected node is a graph group node instead of a contact, the detail panel should show a group summary and a list of member contacts rather than editable contact fields.

### 16.3 Light / Dark Theme

The app supports a light and a dark theme, toggled from a header control.

- Themes are pure stylesheet overrides: the stylesheet defines the dark palette as the default token set and a light override keyed off a theme attribute on the document root. The toggle only flips that attribute.
- The default theme is dark. The current choice persists across reloads (a local-storage key; see appendix); a missing or unreadable value falls back to dark.
- Because category colors are stylesheet-driven (§11.2), switching theme recolors the sidebar and graph by refreshing the palette cache and re-reading the tokens — no graph rebuild.
- The toggle button's label announces the theme you will switch *to*.

## 17. Error Handling Requirements

The app should degrade safely when:

- a related name matches multiple contacts
- a raw vCard line cannot be patched exactly
- a card lacks expected Apple fields
- a single record in an import is malformed
- a Markdown contact references a photo file that was not supplied
- browser persistence fails

Safe degradation means:

- do not silently bind to the wrong person
- avoid crashing the detail panel
- isolate per-record parse failures — skip the one bad record with a console warning and still import the rest (do not abort the whole import)
- report unresolved Markdown photo references as a count, keeping the contacts themselves
- preserve as much imported data as practical
- surface failures via console and/or unobtrusive UI messaging if added

## 18. Accessibility Requirements

A reimplementation must meet at least the following accessibility contract:

- **Keyboard-focusable graph nodes.** The graph canvas is labeled as a group with usage instructions ("Use Tab to move between contacts, Enter to open one"). Every node element is focusable (`tabindex="0"`), exposed as a button (`role="button"`) with the contact's name as its accessible name (`aria-label`), and activates (selects/opens) on Enter or Space.
- **Modal dialogs.** Each modal is marked `role="dialog"` with `aria-modal` and an `aria-labelledby` title. Opening a modal moves focus to its first useful visible control (skipping hidden value-holder elements). While a modal is open, Tab/Shift-Tab is trapped within its focusable elements, and Escape closes it (returning the user to the underlying page).
- **Combobox pattern on searchable selects.** The type-to-filter enhancer exposes its text input as `role="combobox"` with `aria-autocomplete="list"`, `aria-expanded`, and `aria-controls` pointing at the floating listbox; list options carry `aria-selected`. The hidden native select is `aria-hidden` and removed from the tab order. Keyboard: ↑/↓ move the active option, Enter selects, Escape closes.
- **Menu-button pattern on popover menus.** Menu triggers carry `aria-haspopup="menu"` and a live `aria-expanded` state; menus close on selection, outside click, Escape, scroll, or resize.
- **Visible focus.** All interactive elements (links, buttons, inputs, selects, textareas, tabindexed elements, custom toggles) show a visible focus ring via `:focus-visible`, in both themes.
- **Theme toggle.** The theme control is a real button whose label states the theme it switches to.
- **View toggle.** The graph/table segmented control exposes its buttons with `role="tab"` semantics and an active state.
- Icon-only controls (graph zoom/fit/reset/re-layout, detail-header buttons) carry `aria-label`s.

## 19. Suggested Internal Module Boundaries

A clean reimplementation should keep responsibilities separated roughly as follows (the current codebase's concrete file layout is in the appendix):

### 19.1 Parser Module

Responsibilities:

- read vCard text
- unfold lines
- parse supported properties
- normalize Apple-specific structures
- output contact models

### 19.2 Relationship Builder Module

Responsibilities:

- resolve names where safe
- create virtual contacts
- generate graph nodes/edges
- generate mode-specific group nodes and cluster hull metadata
- assign primary category and filter tags
- compute family-network membership

### 19.3 Graph View Module

Responsibilities:

- render/update force graph
- depend on a locally vendored force-graph library (currently D3 v7)
- apply filters
- manage selection callbacks
- render node visuals
- render cluster hulls
- distinguish contact nodes from group nodes visually
- maintain per-render indexes for node and edge lookup
- stay decoupled from the controller (communicate through a small event API)

### 19.4 App Controller Module

Responsibilities:

- bind DOM events
- own current state
- manage detail panel rendering
- handle edit/save flows
- regenerate backing cards via the shared serializer
- import/export
- session persistence
- maintain derived lookup indexes for graph/detail/table operations

The data layer should have single-source modules that everything else derives from, so a fact is defined exactly once:

- the **relationship taxonomy** (§10.5): types, display/vCard labels, categories, reciprocals, normalization, gender groups, and the picker options — parser, builder, and controller all delegate to it
- the **standard contact field registry** (§8.1): the canonical contact field shape that the parser, adapters, and graph node builder derive their field lists from
- the **contact-method type taxonomy** (§9.4): the user-facing TYPE lists for emails/phones/urls/addresses
- the **color palette** (§11.2): theme-stylesheet tokens read through one cached palette accessor
- the **vCard serializer** (§13.1): the single write path for all card regeneration

Shared UI helpers worth isolating: a progressive enhancer that turns any native select into a type-to-filter combobox while preserving its value and change event (§18), and a menu-button helper that turns a button into an accessible dropdown-menu trigger (§7.2).

### 19.5 Runtime Indexes and Invalidation

The app intentionally favors data preservation and correctness, but it should avoid repeated full-array scans in hot UI paths. A reimplementation should maintain these indexes:

- `contactById`: maps real contact IDs to mutable contact objects.
- `contactsByUid`: maps vCard `UID` values to contacts for stable session restore.
- `contactsByFn`: maps normalized display names to contacts for duplicate checks and fallback restore. If duplicate display names must be supported, this should become a multi-map plus ambiguity checks.
- `nodeById`: maps current graph node IDs to graph nodes, including virtual and group nodes.
- `edgesByNodeId`: maps a node ID to all current graph edges incident to that node.
- `relatedRefsByTargetId`: maps a target node ID to reverse relationship references from contacts that list that target.

Indexes are derived state and must be rebuilt after any operation that can change contacts, graph nodes, edges, or relationship resolution:

- after import
- after session restore, before resolving saved self-contact references
- after creating/deleting contacts
- after editing contact fields, names, notes, tags, photos, addresses, or company status
- after adding/editing/deleting relationships
- after changing graph mode or graph visibility options

The graph renderer should keep its own filtered `nodeById` and `edgesByNodeId` indexes for the currently visible graph. Selection highlighting, selected-node zooming, connected-edge highlighting, and cluster hull label placement should use those indexes rather than scanning the full render arrays on every interaction or tick.

## 20. Acceptance Criteria

## 20.1 Import / Parse

- Importing a multi-contact Apple VCF produces one contact per card.
- Importing multiple Markdown files in one operation combines single-contact and bundled Markdown contacts into one working set.
- Importing a Markdown export together with its externalized image files re-attaches each photo by filename; missing photo files are reported, not fatal.
- Contacts with `N:` but no `FN:` still appear with a usable display name.
- Photos, URLs, addresses, and structured names populate the UI correctly.

## 20.2 Relationship Safety

- If two real contacts share the same name, the app does not auto-link a related-name entry to the wrong one.
- Unresolved names produce virtual contacts.

## 20.3 Editing

- Editing a contact’s name updates both display and structured name fields in export.
- Editing relationship type and saving survives reload/restore.
- Editing or adding a photo updates the graph immediately.
- Editing emails/phones/addresses/websites preserves types and preferred state.
- Untouched contact-method instances survive a rewrite byte-for-byte (original TYPE casing and parameter order preserved).
- Checking `Treat as Company` round-trips through export.
- Editing ordinary contact fields preserves unmodeled Apple date item groups and the alternate-birthday line.
- A rewrite never emits duplicate `GENDER` lines or stale `X-CONSTELLATION-FIELD` payloads.
- Address edits preserve PO-box and extended-address components.
- Exported folded lines preserve non-ASCII text while staying within vCard byte limits.

## 20.4 Family Filter

- Selecting a “me” contact changes the `Family` filter membership based on explicit connectivity only.
- A disconnected family-labeled cluster is excluded.
- A connected non-family-labeled cluster is included if explicitly reachable.

## 20.5 Persistence

- After import, edit, and “me” selection, reloading and clicking `Restore Last` restores the edited dataset and selected “me” card.
- The selected graph view is restored with the saved session.

## 20.6 Graph/UI Integrity

- Selecting one node after another updates the full detail panel, not just the header.
- Full names render on graph labels.
- `Add Relationship` appears at the bottom of the `Relationships` section.
- The sidebar contact list supports both `First Last` and `Last, First` sort modes.
- The contact-list sort label and dropdown appear on the same row.
- In `Last, First` mode, the sidebar displays names in `Last, First` format whenever structured name data is available.
- Contact-list row accents are derived from the contact's filter tags rather than a single legacy category color.
- The sidebar is split into a controls column and a dedicated contacts-list column.
- The controls column can be collapsed while the contacts/search column remains visible.
- Detail panels show custom fields from `customFields` / `record.fields`.
- Detail edit mode can edit scalar custom fields and list custom fields; nested object custom fields are displayed read-only and preserved.
- Switching graph views does not require reimporting contacts.
- Switching between graph and table mode does not require reimporting contacts.
- `Connections` can show or hide organization clustering independently.
- `Connections` can show or hide likely family independently.
- `Connections` can show or hide likely connections independently.
- `Connections` only creates surname clusters when 2 or more contacts share the same family name.
- `Connections` only creates hashtag clusters when 2 or more contacts share the same hashtag.
- Organization clusters are only inferred for orgs with 2–100 members.
- `Connections` uses clearly unconfirmed visual treatment for likely group links while still showing explicit relationship edges as confirmed.
- `Geographic Relationships` groups contacts by preferred address using the hierarchy country → state/province → city → street.
- Grouped graph views render prominent hull labels naming each cluster.
- Hull labels remain readable when the graph is zoomed out.
- Group nodes show a group summary in the detail panel instead of editable contact details.
- Custom relationship labels and contact-provided link fields cannot inject executable HTML into the detail panel.
- Graph nodes are keyboard-focusable and activate with Enter/Space (§18).

## 21. Recommended Test Matrix

The repository includes an offline Node unit test suite plus a browser end-to-end smoke suite, runnable from the repository root:

```bash
npm test          # node:test unit suites
npm run test:e2e  # Playwright end-to-end smoke suite (chromium)
```

The automated unit suite lives under `test/` and uses fixture VCF files under `fixtures/`; the e2e smoke suite lives under `e2e/` and drives the served app in a real browser (it also runs as a CI job). The primary fixture is `comprehensive.vcf`, which covers:

- normal Apple-style contacts
- duplicate display names
- photos
- custom type labels
- preferred email, phone, and address fields
- escaped semicolons, commas, and newlines
- explicit relationships
- unresolved relationships that create virtual contacts
- company contacts via `X-ABSHOWAS:COMPANY`
- Notes hashtags
- geographic address data

Automated tests must cover:

- parser import of all fixture fields
- serializer round-trip: parse fixture → rewrite/serialize → reparse → verify key fields and metadata survive
- duplicate-name ambiguity behavior
- virtual contact graph generation
- saved "me" contact restore by `UID`
- table notes edit and immediate searchable data refresh
- detail-field rewrite preserving metadata
- relationship add/edit/delete and relationship type persistence
- bulk normalize append and replace behavior, including appending to empty Notes
- photo update serialization
- Notes hashtag autocomplete context and insertion
- contact deletion from the working export set
- vCard import attaches a format-neutral `ContactRecord`, `customFields`, and `sourceDocuments`
- `ContactRecord` stays synchronized after legacy contact edits
- `VCardAdapter` imports, serializes, filters selected contacts, and recognizes vCard file extensions
- `MarkdownAdapter` imports frontmatter/body contacts, preserves arbitrary typed fields, supports bundle files, and serializes Markdown output
- `TsvAdapter` parses a row into the contact model, serialize→reparse round-trips the common fields, escapes tabs/newlines, emits a header+example template, and isolates a malformed row
- relationships resolve by `uid` when present (ignoring a mismatched name) and fall back to name matching when absent
- deterministic contact ids are stable across reparses and distinct for duplicate names
- non-`company` tags round-trip through the vCard fallback via `CATEGORIES`
- robust sample Markdown fixtures import as separate single-contact files and as a multi-contact bundle
- multi-file import combines supported Markdown files into one working set
- Markdown import → export → reimport preserves unknown fields, unknown nested objects, null/empty values, and Markdown body content
- vCard → Markdown and Markdown → vCard conversion preserve standard contact data
- custom fields render in read-only detail mode
- scalar and list custom fields can be edited while object custom fields are preserved
- preservation of non-anniversary Apple date item groups during contact rewrites
- HTML escaping and unsafe-href rejection for user/contact-provided display values
- UTF-8 byte-safe vCard line folding
- a malformed vCard / Markdown / TSV record is skipped without aborting the import
- e2e smoke: the app serves, imports a fixture, renders the graph, opens a contact, and exports

Manual Apple Contacts validation is documented in `docs/APPLE_CONTACTS_ROUNDTRIP_CHECKLIST.md`.

At minimum, automated or manual coverage should include:

- VCF with `FN` and `N`
- VCF with `N` but no `FN`
- duplicate contact names
- contact with no photo, then add photo
- contact with multiple emails and one preferred
- contact with custom type labels
- relationship rename to an existing contact
- relationship rename to a new freeform name
- relationship type edit followed by full save and restore
- sidebar sort mode toggle followed by save and restore
- contact-list row coloring reflects system tags and hashtags
- graph view switch followed by save and restore
- table mode switch followed by save and restore
- inline table edit of name/org/title
- inline table edit of notes updates hashtag tags
- add contact from table mode
- delete contact from table mode
- connections view with organization clustering on/off
- connections view with surname clustering on/off
- connections view with hashtag clustering on/off
- connections surname cluster with 2 contacts
- connections surname with only 1 contact does not create a cluster
- hull label appears for connections clusters
- hull label remains readable after zooming out
- connections hashtag cluster with 2 contacts
- connections hashtag with only 1 contact does not create a cluster
- geographic grouping across country/state/city/street
- virtual contact conversion to real contact
- “me” selection restore after reload
- family filter on disconnected clusters

## 22. Future Extension Opportunities

Possible next steps for a new implementation:

- formal schema validation for parsed contacts
- richer conflict UI for ambiguous duplicate-name relationships
- better preservation of obscure Apple/private fields
- a CSV variant of the TSV adapter (the schema/encoding already factor cleanly on a delimiter)
- optional import/export diff view
- browser-side undo/redo stack (currently mitigated by session persistence + Export All)
- import merging — loading a new file currently replaces the working set; merge logic would need conflict resolution
- more graph modes (timeline view, org-chart layout)
- graph export (PNG/SVG of the current view)
- persistence for dismissed suggestions (currently lost on reload)

The following directions from earlier drafts are now **implemented** and no longer future work: native ES-module delivery, splitting the monolithic controller into mixin modules, incremental position-preserving graph rebuilds, UID-first relationship resolution, the TSV adapter, the light/dark theme, Markdown re-import with externalized photo re-attachment, relationship editing from table mode, gender assignment, and the consolidated single vCard serializer.

---

## Appendix: Implementation notes (current JS codebase)

Everything above is platform-neutral. This appendix maps the spec onto the current vanilla-JS implementation for maintainers of this repository. Verified against commit d7a9715 (2026-07-02).

### A.1 Delivery and entry point

`index.html` loads exactly two scripts: vendored D3 v7 as a classic `<script src="js/vendor/d3.v7.min.js">` (browser global), then `<script type="module" src="js/app-bootstrap.js">`. The static `import` graph (via `js/app-controller.js`) resolves every other module; `package.json` declares `"type": "module"`. D3 v7 is required (uses `d3.zoom()`, `d3.forceSimulation()`, `d3.polygonHull()`, `d3.drag()`). `npm install` pulls dev tooling only (ESLint, Prettier, Playwright); the app itself has no runtime dependencies.

### A.2 Module map

Data layer / shared singletons:

| File | Responsibility |
|---|---|
| `js/vcard-utils.js` | `VCardUtils` — RFC 6350 unfold/escape/fold/parse helpers + `contactMethodKey` (§9.7) |
| `js/dom-utils.js` | `escapeHtml` — the single HTML-escaping helper (text + quoted-attribute safe), used by `app*.js` and `graph.js` (§13.6) |
| `js/palette.js` | `Palette` — reads the CSS `--cat-*` tokens via `getComputedStyle` (cached); single color source (§11.2) |
| `js/contact-record.js` | `ContactRecord` — format-neutral record, `STANDARD_FIELDS` shape registry, `createEmptyContact()`, `assignStableId()` (§8.1) |
| `js/contact-types.js` | Shared contact-method TYPE taxonomy for emails/phones/urls/addresses (§9.4) |
| `js/relationship-taxonomy.js` | `RelationshipTaxonomy` — types/labels/vCard labels/categories/reciprocals/aliases/`GENDER_GROUPS`/`genderedReciprocal`/`isReciprocalDowngrade`/picker options (§10.5). `scripts/gen-taxonomy-doc.js` generates the §10.5 tables from it |
| `js/vcf-parser.js` | `VCFParser` — vCard text → contact models |
| `js/vcard-serializer.js` | `VCardSerializer` — the single vCard writer: `rewriteVCard(contact)` (hybrid raw-preserving rewrite) and `generateVCard(contact)` (full regeneration), sharing `_modelLines`; `MODELED_PROPS` lists the regenerated properties (§13.1) |
| `js/vcard-adapter.js` | vCard format adapter (delegates to `VCFParser` / `VCardSerializer`) |
| `js/markdown-adapter.js` | Markdown frontmatter/body adapter (+ `photoMap` import, `serializeBundle()` → `{ markdown, images }`) |
| `js/tsv-adapter.js` | TSV adapter (`COLUMNS`, `templateText()`) |
| `js/relationship-builder.js` | `RelationshipBuilder` — contacts → `{ nodes, edges, hulls }`; name/UID indexes, `findRelationTarget`, BFS family network |
| `js/graph.js` | `ConstellationGraph` — D3 renderer, decoupled from the controller via a small `on`/`emit` API |

Controller — **one class (`ContactRelationshipApp`) split across mixin modules**. `js/apply-mixin.js` grafts a mixin class's methods onto the prototype; because all mixins extend the same prototype, `this._foo()` resolves across module files with no call-site changes. `js/app-controller.js` imports `app.js` plus every mixin (for side effects) and re-exports the class; both the browser entry and the test harness import from there. The split is deliberately mechanical and does not change state ownership.

| File | Methods for |
|---|---|
| `js/app.js` | Core: constructor, `_init` (DOM wiring, menus, modal a11y, collapsible panels), file load (`_loadFiles`, adapter pick, sibling-image `photoMap`), the `_rebuildGraph` / `_reindexContacts` / `_reindexGraphData` pipeline, shared helpers |
| `js/app-notes.js` | Notes inline save + hashtag autocomplete |
| `js/app-session.js` | IndexedDB persistence + "me" self-contact resolution |
| `js/app-sidebar.js` | Contact list, filters, legend, stats, tag colors |
| `js/app-table.js` | Editable table view (incl. delete-selected) |
| `js/app-detail.js` | Detail panel render / node select / collapsible sections |
| `js/app-suggestions.js` | Suggestion engine (§10.6; `INWARD_TRANSITIONS` lives here) |
| `js/app-editing.js` | Edit mode, field editors, inline read-mode editing (`_editableDetailRow` / `_enterInlineFieldEdit`), `_rewriteEditableFields` → `VCardSerializer.rewriteVCard` |
| `js/app-relationship-edit.js` | Inline relationship CRUD + add-relationship modal |
| `js/app-bulk.js` | Bulk-normalize rule engine + modal |
| `js/app-export.js` | Multi-scope export, `_exportAllIndividualVCards`, `_saveFilesSeparately` (directory picker fallback), TSV template |
| `js/app-gender-wizard.js` | Gender Assignment wizard modal (§5.8) |
| `js/app-theme.js` | Theme toggle: flips `data-theme` on `<html>`, `Palette.refresh()` + `graph.refreshColors()` |
| `js/app-bootstrap.js` | Entry module: `DOMContentLoaded` startup + modal/global wiring; exposes `window.app` |

Shared UI helpers:

- `makeSearchable(select, { placeholder })` (`js/searchable-select.js`) — progressively enhances a native `<select>` into the §18 combobox. The select stays in the DOM as the hidden value holder, so `select.value` and its `change` event keep working. Idempotent (`refresh()`), self-cleans its document listener when detached, tolerates a not-yet-inserted select (caller appends the returned wrapper). Gotcha: `classList.toggle(cls, undefined)` *flips*, so callers must pass real booleans. Applied to all large pickers (add-relationship contact + type, bulk field/type/from/to, suggestion type); small 2–3 option selects stay native.
- `attachMenu(trigger, itemsOrFn)` (`js/menu-button.js`) — dropdown-menu trigger; items are `{ label, onSelect, disabled?, danger? }` or `{ separator: true }`, evaluated per open when a function. The popover is fixed-positioned (never clipped by `overflow:hidden` ancestors) and closes on selection / outside click / Escape / scroll / resize. Used for Import ▾, Export All/Selected/Contact ▾, Session ▾.

### A.3 Implementation-only constants and conventions

- **Stable ids**: `c_` + FNV-1a hash over `uid:<uid>` (or `fn:<fn>`) plus an occurrence suffix for duplicates. Virtual nodes: `id = 'virtual__<sanitized_name>'`.
- **IndexedDB**: database `constellation-db`, store `sessions`, key `constellation:last-session`. Payload keys: `fileLabel`, `formatId`, `content`, `savedAt`, `selfContactRef`, `showInferred`, `showLikelyFamily`, `showLikelyConnections`, `showIsolated`, `showVirtual`, `suggestExtendedFamily`, `sidebarControlsCollapsed`, `contactSortMode`, `graphMode`, `mainViewMode`.
- **Theme**: localStorage key `constellation:theme`; CSS tokens on `:root` (dark default) with overrides under `:root[data-theme='light']` in `css/styles.css`. Representative dark tokens: `--cat-family` #e17055, `--cat-friend` #00b894, `--cat-work` #74b9ff, `--cat-neighbor` #fdcb6e, `--cat-company` #8e44ad (a violet reserved for companies), plus `--cat-node-default` / `--cat-group` / `--cat-selected` / `--cat-edge-inferred`; the stylesheet is authoritative.
- **Geo hull colors** by depth: `{1:'#74b9ff', 2:'#55efc4', 3:'#fdcb6e', 4:'#fd79a8'}`.
- **Edge dedup**: explicit mode uses a `pairSet` (`id1↔id2`, one edge per pair); org-inferred edges use `edgeSet` (`id1↔id2:type`); geographic mode dedups by `pairKey:edgeKind`.
- **Positional vCard indexing**: the parser pre-extracts raw blocks and base64 photos into positional arrays (photo bodies replaced with `PHOTO:__stripped__` before unfolding for speed); block *i* always pairs with raw/photo *i*, avoiding FN-collision overwrites.
- **Raw-line capture for rewrite**: at parse time, each multi-value instance's original raw line(s) are captured in `contact._rawByKey`, keyed by `VCardUtils.contactMethodKey`; `VCardSerializer.rewriteVCard` re-emits untouched instances from it byte-for-byte (§13.1).
- **SVG structure**: five layered `<g>` groups (hulls, hull-labels, links, nodes, labels); defs hold per-category arrow markers (`arrow-family` …) and a Gaussian-blur `glow` filter for selection. Each edge `<g>` holds a `<line>` + two `<text>` labels; each node `<g>` holds selection ring, main shape (`circle`, or `rect` with rounded corners for companies — both classed `node-circle`), optional clipPath+image (photo), and initials/company/group glyph text.
- **Re-layout** (`graph.relayout()`, header `❖` button): clears `_nodePositions` and pins, re-scatters nodes within ±`min(dimension,600)/2` of center, restarts at alpha 1, and auto-runs `fitView()` after 1.4 s.
- **Build stamp**: the `.build-stamp` block in `index.html` (Updated / date / time) is a hand-edited dev marker, **hidden from the UI** (`display:none` in CSS) but kept in the DOM and still updated on every edit run. The stylesheet link carries a `?v=` cache-buster that must be bumped when `styles.css` changes.
- **`_rewriteEditableFields(contact)`** is the single controller chokepoint: mutate the model → call it → it delegates to `VCardSerializer.rewriteVCard` and then `_syncContactRecord(contact)` refreshes the attached `ContactRecord`. Relationship add/edit/delete and suggestion-apply only mutate `contact.related` and call this — no raw string surgery anywhere.
- **Bulk retype note**: the relationship-type THEN action still uses dedicated "from type → to type" controls (`#bulk-rel-from`/`#bulk-rel-to`) rather than the plain Set/Clear form; reworking it is deferred pending a design decision.

### A.4 Tests, tooling, CI

- `npm test` — Node's built-in `node:test`. `test/helpers/load-app.js` installs fake browser globals (`document`/`window`/`indexedDB`) on `globalThis`, then imports the ES modules directly, so parser/builder/serializer/adapter/controller methods run without a browser. Fixtures in `fixtures/` (primary: `comprehensive.vcf`, plus Markdown samples).
- `npm run test:e2e` — Playwright smoke suite in `e2e/` (`smoke.spec.js`, `global-setup.js`), chromium; runs as a GitHub Actions CI job alongside unit tests, lint, and format checks.
- `npm run lint` (ESLint flat config, ESM) and `npm run format` / `format:check` (Prettier). None require network access.
- Browser requirements: ES modules, IndexedDB, FileReader, Blob/`URL.createObjectURL`, CSS Grid, `localStorage`; optionally the File System Access API (`showDirectoryPicker`) for grouped Markdown+image export and individual-vCard export (both fall back to sequential downloads).
- Dev serving: any static server over `http://`, e.g. `python3 -m http.server 7891` from the repo root.
