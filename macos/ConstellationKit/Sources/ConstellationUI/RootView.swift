import SwiftUI

/// Root scaffold view for the Constellation app. Real UI (graph canvas,
/// sidebar, table view, etc.) lands in later milestones.
public struct RootView: View {
    public init() {}

    public var body: some View {
        Text("Constellation")
    }
}
