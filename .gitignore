# --- macOS ---
.DS_Store
.AppleDouble
.LSOverride

# --- Personal contact data (never commit real exports) ---
# Synthetic test fixtures under fixtures/ are intentionally
# NOT ignored (these patterns are anchored to the repo root only).
/*.vcf
/*.vcard

# --- Archives / snapshots ---
*.zip

# --- Node / tooling ---
node_modules/
npm-debug.log*
yarn-error.log*
.eslintcache

# --- Playwright (artifacts normally go to tmpdir; belt-and-braces) ---
test-results/
playwright-report/

# --- Local editor / agent settings ---
.claude/
.vscode/
.idea/

# --- macOS native app (generated / build artifacts) ---
macos/Constellation.xcodeproj/
macos/ConstellationKit/.build/
macos/DerivedData/
